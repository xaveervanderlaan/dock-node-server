const http = require("node:http");
const router = require("find-my-way")();
const path = require("node:path");

export default class laanRouter {
  constructor() {}

  forgeDefaultViews() {
    const viewPath = "asd";
  }
}
